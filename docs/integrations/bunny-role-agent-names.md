# Bunny role-visible agent names

- Intent: Neon manual Bunny workers appeared as indistinguishable `general-purpose` agents despite distinct hunter/recon roles. Give the operator role-and-surface identifiers in Claude Code's agent manager.
- Branch/worktree: `feat/bunny-role-agent-names` at `../bunny-role-agent-names`; original base `origin/beta` `d077b8971b3e87fbd1278f4874cd53399564b74f`; reconciled to `origin/beta` `4f607df`; target `beta`.
- Contract: The Bunny coordinator invokes role-specific Claude Agent `subagent_type` values (`bunny-hunter`, `bunny-recon`, `bunny-verifier`, `bunny-reporter`) supplied by the skill's `agents/` definitions. Each Claude host projects these into agent discovery; scoped descriptions identify surfaces. No tool `name` field is required. This does not alter role authority, scope, or goal state.
- Source evidence: Hoster's Neon Bunny run `neon-bunny-extpriv-20260925T0549Z` used three `general-purpose` worker calls. Hoster has Claude Code 2.1.282. A local Claude 2.1.280 smoke with `name` requested on a generic agent omitted that field, so the initial instruction-only proposal was rejected after independent review. A fresh non-target local Claude session with the four definitions symlinked into a scratch project's `.claude/agents/` invoked `bunny-recon` as `subagent_type`, returned `ROLE_SMOKE_OK` with zero tool uses, and reported `by_type: {"bunny-recon":1}`.
- Verification: run focused Bunny tests and `git diff --check`; independently review the revised diff. After beta publication, project the four definitions on Hoster from its resolved beta skill, then start a fresh non-target Claude invocation and inspect its Agent `subagent_type` and result. Do not interrupt the ongoing Neon run.
- Review: Independent reviewer approved corrected commit `5b424024` for beta integration after 4 focused tests and `git diff --check`, but explicitly withheld Hoster activation until projected definitions and fresh 2.1.282 smoke are verified. Beta's intervening browser-repair changes did not overlap this feature; merge of `4f607df` into this branch and focused rerun passed.
- Activation boundary: the feature branch is reviewed for beta integration, not yet live. Publication, Hoster beta skill refresh and Claude agent definition links are separate. Existing running agents retain their names.
- Deferred: dispatch-readiness rule and program-shared request admission queue are separate tasks, not part of this change.
- Next: validate/review, merge and publish beta if clean, refresh Hoster beta projection, link the four Claude agent definitions, and verify a fresh non-target invocation uses its role-specific type.
