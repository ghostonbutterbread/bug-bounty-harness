# Browser program policy + fixture peer generation integration dossier

- **Owner / branch / worktree:** canonical `feat/browser-auth-policy-fixture-integration` at `/home/ryushe/projects/bug_bounty_harness-browser-auth-integration`; reviewed parent `418c17a`, reviewed peer `02258b3` merged with ancestry preserved. Intended next integration target is `beta` only after a separate combined-tree review; fetched `origin/beta` `4297609` but did not merge or push it.
- **Scope:** private manager-only fixture promotion/pending readback and fail-closed apply for exact `(fixture, fixture.invalid, anon)` and `http://localhost:<port>`. Promotion loads the mode-0600 manager-selected `/me` contract; its native source probe checks exact principal/URL/status/no redirect, approved localStorage and the browser-enforced Secure/Path=/ `__Host-session` cookie under a ticket bound to transaction, owner and original CDP generation. Lost begin is replayed once; lost end requires exact identity/availability readback. An unproved release publishes no generation and retains the source fence for operator reconciliation. Ledger stores no auth secret. No production site adapter or public API.
- **Recipient boundary:** direct transfer remains an unmanaged empty-recipient bootstrap, gated by exact pool/origin/contract/cookie and canonical identity; every ledger-enrolled recipient (`pending`, `applying`, `applied`) is denied before candidate/browser access. Forged prior token/lock/source-identity kwargs fail at signature. `manager_fixture_apply` has no authenticated recipient caller channel and returns `recipient-approval-required` without touching peer state; caller labels and Boolean approval are not authority. A promotion does not silently consume stopped peers.
- **Merge resolution:** retained parent site contract, source-end/activation/finalize terminal disposition and public-CDP fence; ported peer source-ticket recovery, generation ledger and admission check to the same localhost contract. Parent and peer predecessor receipts below are historical, not claims about this merged tip. No beta merge/push, Hoster or live account touched.
- **Combined verification (canonical worktree, `PYTHONPATH=$PWD:$PWD/agents`, local Python):** bounded pytest batches: peer promotion/ledger + site contract + policy **43 passed, 29 subtests**; terminal fence/activation/begin/end **62 passed, 4 skipped**; source-end/quarantine/races/manager gate/auth fence **80 passed**; provisioner/profile lease/resources **127 passed**. Opt-in `BBH_AUTH_CANARY=1` two-disposable-Chrome canaries **14 passed** in bounded batches (ordinary promotion and fail-closed missing storage, unmanaged empty bootstrap, principal/redirect/cookie negative, end/commit/begin loss and activation/finalize fences). An initial native four-case run failed at the new storage-negative assertion because canonical transfer correctly returns `source-storage-unverified`, not the peer's old `source-app-check-failed`; the assertion was corrected and all cases passed on rerun. The promotion failure itself returns `source-app-check-failed`. No production caller or live account was exercised. Independent combined-tree review is a separate beta gate; no real-site completeness, logout/revocation invalidation, startup consumption, manager-authenticated recipient approval or live owner/data preflight. A preexisting `applying` marker remains blocked for manual review.

## Inherited predecessor dossier at `d3cbde8` (historical; current status above supersedes)

- **Status:** reviewed fixture-only site-contract descendant `e32383f` merged back into canonical parent; integrated verification below. **Not** a production authenticated clone pool or beta-ready approval.
- **Owner / canonical path:** Hermes integration / `docs/integrations/browser-auth-clone-pool.md` on `feat/browser-auth-policy-fixture-integration` at `/home/ryushe/projects/bug_bounty_harness-browser-auth-integration`.
- **Base / intended target:** canonical parent `d3cbde89ebea7515a2b460922f6c192500859b65`, source descendant `feat/browser-auth-site-fixture-integration` at `e32383f039dcce06cd54f86455453a9f4a46409d` (two unique commits `cb76e94`, `e32383f`), now merged preserving history into parent. Eventual `beta` is a separate gate. Fetched `origin/beta` at `4297609753de3e1eb5fda3858dec895c6162b3fd`, not merged here. No beta merge/push, Hoster, or live-account operation.
- **Implementation ancestry:** policy predecessor `feat/browser-auth-clone-pool` `eaaeddb` (policy commits `814cb64`, `404ec51`, `f1df3a4`, `442f288`); fixture predecessor `feat/browser-auth-fixture-transaction` `ba2845c` (fence and manager/adapter commits `8661c9b`, `a88d652`, `2c1b73a`, `f6d9fb3`, `ee578fa`, `9466fe0`). Both histories are retained through merge commits `149dae2` and `f2600ce`; `origin/beta` is an ancestor. Prior dossiers remain recoverable at `eaaeddb:docs/integrations/browser-auth-clone-pool.md` and `ba2845c:docs/integrations/browser-auth-transfer-fence.md`. This replaces those branch-local dossiers in the combined tree, not their Git history. The product contract is `docs/browser-auth-clone-pool-spec.md`.
- **Inspiration:** program-scoped same-color concurrent browser request, not the existing isolated-empty auto-slot behavior. A read-only architecture packet found no real site with a provably complete transfer contract.

## Implemented boundaries

The program-scoped `single`/`multiple` policy is stored by exact program with categorical evidence and CLI set/show readback. Its common admission predicate covers provisioner selection and canonical acquire/transfer: program `single` dominates exact-account `multiple`; exact-account/domain `single` dominates program `multiple`; unknown/absent program mode preserves the legacy exact-account behavior. The policy does not turn an unauthenticated browser into a clone and does not silently broaden another program/domain/account. Active leases are not changed by policy writes; logout remains passive evidence, not a switch to `multiple`.

The canonical private transfer identity/grant tables default to **no attestation** for existing rows. Manager attestation does not issue production grants. `manager_fixture_auth_transfer` is a private Python fixture transaction, not a CLI/HTTP/general account endpoint. It accepts only manager-derived owners matching exact `(fixture, fixture.invalid, anon)` and an exact loopback `http://localhost:<port>` origin, with a manager-selected fixture `/me` DOM principal check, one `__Host-` HttpOnly cookie, and one selected-origin localStorage key. It refuses mismatched owners, program, domain, account, origin, physical profiles, process/unit and CDP identities. Source and destination use separate live Chrome profiles. Exclusive adapter tickets and a quarantine/commit/activate sequence fence public CDP across transfer and uncertain replies; exact destination disposal is attempted when the end/commit outcome is uncertain. A failed exact stop remains `destination-cleanup-incomplete` and requires operator disposal; no source stop or raw profile copying. No caller-provided identity, auth payload, script, or site definition is accepted. No pool generation, peer promotion, or production site adapter is implemented.

Latest `origin/beta` browser-history repairs were reconciled at `4840f82`; the fetched `d077b89` base also carries generic startup diagnostics. Git reported no textual conflicts; semantic overlap in `browser_profile_lease.py` and `browser_provisioner.py` was checked with the combined suites. One existing `PipeBrowser.__new__` test fixture needed initialization for the new transfer/quarantine state; production fallback was not loosened. The independent review then reproduced an applied source begin whose reply was lost: the manager had no ticket to release and stranded the source public CDP. The repair supplies a manager-generated private transaction identifier tied to exact owner lease and original CDP generation, idempotent begin replay, and ticket/transaction/owner/generation matching for every privileged operation. A recovered begin is explicitly released without importing any auth; double failure returns a side-specific cleanup-incomplete status unless exact adapter identity proves it available. The source is never stopped to resolve begin uncertainty.

## Descendant site-contract and propagated source-end repair

The inactive site-contract validator from `feat/browser-auth-site-contract` implementation `54d8284` is copied into this descendant without merging that divergent branch. The private manager fixture reads a mode-0600 manager-selected contract file at `STATE.parent/fixture-site-contract.json`, binds the exact fixture pool and `http://localhost:<port>` origin, and retains all existing manager identity and exclusive-ticket fencing. No agent-supplied path, script, principal or cookie selection is accepted. The canary serves a DOM-bearing `/me` endpoint; fixed browser-side no-follow fetch parses an exact restricted selector and the validator rejects redirect, response URL/status or principal mismatch. The selected cookie is `__Host-session` with browser-enforced Secure/Path=/ host-only semantics; `Network.getAllCookies` lacks authoritative host-only metadata, so non-prefix/ambiguous cookies fail closed. See `docs/browser-auth-site-contract.md` and the canonical spec for the boundary. This is **not** production site transfer, peer promotion, or real-account authority.

The parent repair `0da17a8` is merged rather than duplicated. It aims to fix an imported recipient stranded after an applied source `/transfer/end` loses its reply and source release cannot be proved. On handled errors, source identity is checked before destination commit; exact recorded destination disposal is attempted even if rollback was otherwise clean. Failed unit/root/CDP stop proofs remain `cleanup-incomplete`. Post-import source recheck failures likewise aim to keep the recipient private until disposal. The source is never intentionally stopped to recover uncertainty, and responses report only sanitized dispositions. **This is not yet a complete safety claim:** independent review found `KeyboardInterrupt` can bypass disposal, and its failed-stop verifier tests did not exercise the verifiers. The inherited fault suite now uses the descendant's private localhost `/me` contract and `__Host-session` cookie shape.

At `cb76e94`, isolated scratch venv `$TMPDIR/bbh-site-contract-venv` was installed from this worktree root `requirements.txt` with pytest, websocket-client and httpx. Focused `PYTHONPATH=$PWD .../python -m pytest -q agents/test_browser_auth_site_contract.py agents/test_browser_manager_transfer_gate.py agents/test_browser_auth_fence.py agents/test_browser_transfer_begin_uncertainty.py agents/test_browser_transfer_end_uncertainty.py agents/test_browser_transfer_quarantine.py agents/test_browser_transfer_races.py --tb=short` → **73 passed, 29 subtests passed**. Opt-in `BBH_AUTH_CANARY=1 ... -m pytest -q agents/test_browser_auth_native_canary.py --tb=short` → **6 passed** (success, end/commit reply loss, wrong principal, redirect and name-mismatch domain-cookie rejection), two disposable Chromes with source untouched and recipient cleanup. Earlier combined focused run before adapting legacy synthetic fixtures had 11 failures solely from old `127.0.0.1`/`/whoami` and missing private contract setup; adapted fixtures passed. Public transaction dispositions were checked as sanitized; at this predecessor checkpoint `capsys` was unused, so the claimed captured-output assertion was **not** performed. The contract JSON carries only selector/origin metadata. These are predecessor receipts, not evidence for the reconciled tip.

At the predecessor `6f09d3c`, `PYTHONPATH=$PWD $TMPDIR/bbh-site-contract-venv/bin/python -m pytest -q agents/test_browser_auth_site_contract.py agents/test_browser_transfer_source_end_uncertainty.py agents/test_browser_manager_transfer_gate.py agents/test_browser_auth_fence.py agents/test_browser_transfer_begin_uncertainty.py agents/test_browser_transfer_end_uncertainty.py agents/test_browser_transfer_quarantine.py agents/test_browser_transfer_races.py agents/test_browser_program_policy.py --tb=short` → **87 passed, 29 subtests passed**. The opt-in `BBH_AUTH_CANARY=1 PYTHONPATH=$PWD $TMPDIR/bbh-site-contract-venv/bin/python -m pytest -q agents/test_browser_auth_native_canary.py --tb=short` → **6 passed**, using two disposable local Chromes. The new offline same-name `session` negative compares a host-only cookie with `Domain=app.example.org` (same name, domain string and path but broader subdomain scope) and `Domain=.example.org`; both reject via authoritative `host_only=False`, not name mismatch. The real-browser fixture retains `__Host-session`, for which Chrome itself forbids a Domain attribute; its older `session` cookie negative is only a name mismatch and is not counted as same-name scope proof. Passing tests do not override the parent's cancellation/stop-verifier review block.

## Combined-tree predecessor verification (at `3b061ff` and `0da17a8`)

Isolated scratch venv installed from this worktree's root `requirements.txt`; commands ran with `PYTHONPATH=$PWD` and that venv's Python, not an editable sibling installation. The repair's venv is `$TMPDIR/bbh-browser-begin-venv`. No live Hoster/account, retained scratch root, beta merge, or push was touched.

- `pytest -q agents/test_browser_program_policy.py` → **5 passed**. After adding three exact fixture program/domain/account negative cases, `pytest -q agents/test_browser_manager_transfer_gate.py agents/test_browser_program_policy.py agents/test_browser_resources.py --tb=short` → **90 passed**.
- `pytest -q agents/test_browser_auth_fence.py agents/test_browser_manager_transfer_gate.py agents/test_browser_profile_lease.py agents/test_browser_resources.py agents/test_browser_legacy_auto.py --tb=short` → **187 passed** after correcting the synthetic test bridge. An earlier combined run exposed that bridge's missing new attributes; the first oversized run timed out, so verification was split into bounded batches.
- `pytest -q agents/test_browser_provisioner.py agents/test_browser_lifecycle_systemd.py agents/test_browser_transfer_races.py agents/test_browser_transfer_end_uncertainty.py agents/test_browser_transfer_quarantine.py --tb=short` → **49 passed, 2 skipped**.
- `BBH_AUTH_CANARY=1 pytest -q agents/test_browser_auth_native_canary.py --tb=short` → **3 passed** with disposable local browsers and origin; no Hoster/account use.
- Repair RED: `python -m pytest -q agents/test_browser_transfer_begin_uncertainty.py --tb=short` → **4 failed** on the pre-repair source/destination lost-begin seam. GREEN: isolated-venv `pytest -q agents/test_browser_transfer_begin_uncertainty.py agents/test_browser_transfer_races.py agents/test_browser_transfer_quarantine.py agents/test_browser_transfer_end_uncertainty.py agents/test_browser_auth_fence.py agents/test_browser_manager_transfer_gate.py agents/test_browser_program_policy.py --tb=short` → **69 passed**; deterministic fault injection covers applied source and destination begin reply loss, exact release, and explicit cleanup-incomplete when recovery also fails. Adapter negatives prove wrong owner/transaction/generation cannot end another ticket and old public URL remains usable after source end.
- Repair canary: `BBH_AUTH_CANARY=1 PYTHONPATH=$PWD $TMPDIR/bbh-browser-begin-venv/bin/python -m pytest -q agents/test_browser_auth_native_canary.py --tb=short` → **3 passed** with disposable two-Chrome fixtures, safe source readback, recipient cleanup and end/commit reply-loss cases. Existing fixture integration is not a production site contract.
- Source-end repair: `PYTHONPATH=$PWD $TMPDIR/bbh-browser-begin-venv/bin/python -m pytest -q agents/test_browser_transfer_source_end_uncertainty.py agents/test_browser_transfer_begin_uncertainty.py agents/test_browser_transfer_end_uncertainty.py agents/test_browser_transfer_races.py agents/test_browser_transfer_quarantine.py agents/test_browser_auth_fence.py agents/test_browser_manager_transfer_gate.py agents/test_browser_program_policy.py --tb=short` → **77 passed**. Eight new deterministic cases inject lost applied source end with unprovable identity or a failed post-import source recheck, across verified stop and failed unit/root/CDP checks; assert exact destination only, private recipient, source owner intact, and sanitized dual-side status.
- Source-end repair disposable canary: `BBH_AUTH_CANARY=1 PYTHONPATH=$PWD $TMPDIR/bbh-browser-begin-venv/bin/python -m pytest -q agents/test_browser_auth_native_canary.py --tb=short` → **3 passed**. The existing canary exercises disposable real browsers but not the new source-end fault; that seam is deterministic-only pending an independent review decision.
- `git diff --check` and staged diff check were clean; source-end repair plus dossier committed at `0da17a8`.

## Production and peer-promotion gates

No real site currently has a proven safe contract. Keep the exact fixture account/origin hard fence. A future operator-reviewed **site-specific** contract must define browser-native principal validation, required state keys/stores, invalidation on logout/refresh/UI changes, manager-owned control/auth generation, source recheck and destination commit, and failure disposal; do not infer general coverage from a cookie plus localStorage canary. Then add deterministic cross-program/domain/account negatives, concurrent owner/requester and stale-recipient races, generation/revocation checks, source unchanged and recipient rollback, and a disposable site-shaped canary. Verified auth update/peer-promotion to other clones and safe refresh boundaries are not implemented. The offline snapshot predecessor `015ff42` is deliberately **not** merged: do not integrate until a manager no-restart/quiescence proof and independent review exist.

Before any beta merge: independent review of the combined diff, program-policy precedence and fixture-only boundary, source-end uncertainty, activation-ACK and incomplete-stop reconciliation; rerun focused checks on the reviewed tip and integrated tree. Before any production activation: the above site contract and peer-promotion gates, Hoster owner/data preflight, and explicit owner approval. No Blue/live account or Hoster change in this branch. Preserve the older predecessor dossiers in Git history; retire this combined dossier from the beta target only when actually integrated.

## Handoff

- **Parent propagation:** `d3cbde8` corrects the formerly blocked cancellation/disposal and ineffective stop-verifier tests, adds exact activation/finalize readback and a terminal public-CDP fence. Independent parent review approved **fixture-only**, with 82+65 focused (four skipped) and ten native canaries. This descendant merges those commits, not a manually duplicated repair; the old `3b0dbbe` blocker text above is historical predecessor evidence, not the current verdict.
- **Next action:** independently review this reconciled descendant's exact fixture selector, principal and host-only cookie gate against the parent fencing/terminal paths and test receipts; only then consider merge-back into canonical parent. Real-site attestation, manager-owned peer promotion/generation, logout/refresh invalidation and crash/restart journal are unimplemented production gates. Do not merge beta, push, issue production grants, touch Hoster/live accounts, or integrate the offline snapshot.

## Reconciled descendant checkpoint (parent `d3cbde8`)

Merge conflicts were in `skills/chromium-test/scripts/browser_provisioner.py`, `agents/test_browser_auth_native_canary.py`, `agents/test_browser_transfer_source_end_uncertainty.py`, and this dossier. The provisioner takes the parent's activation/readback/finalize/fence/terminal-disposition implementation intact and ports only the descendant's manager-selected mode-0600 localhost `/me` contract, `__Host-session` cookie-scope check and source/destination principal gate. The native test combines parent lost-begin, lost-activation ACK/fence and finalize ACK cases with site-shaped principal/redirect/name-mismatch cases. Source-end and activation fault tests now use the exact localhost contract and cookie shape; stopped-recipient verification and cancellation assertions remain active. `browser_control.py`, new terminal-fence tests and other nonconflicting parent changes merge without override.

Verification from this worktree, `PYTHONPATH=$PWD` and isolated `$TMPDIR/bbh-site-contract-venv/bin/python` installed from the worktree root `requirements.txt` (pytest, httpx and websocket-client): eleven-file focused suite initially **24 failed, 133 passed, four skipped, 29 subtests passed** because a source-end fixture retained a second `127.0.0.1` origin assignment; after removing that stale test-only assignment, its 24-case suite **24 passed**. A final combined rerun of the eleven-file focused suite passed **157 passed, four skipped, 29 subtests passed** in 119.76 seconds. Opt-in two-disposable-Chrome native canary was split into bounded invocations: ordinary transfer **1 passed**; activation fence, lost activation ACK, lost finalize ACK **3 passed**; domain-cookie name mismatch, wrong principal, redirect, unproved source begin **4 passed**; end/commit ACK loss, recovered source/destination begin and unproved destination begin **5 passed**. Thirteen parameter cases in total, including real public-CDP fence/readback and source-owner preservation. One full canary invocation timed out under the tool's 420-second limit without a result; the bounded batches completed. The same-name `session`/Domain offline rejection is covered by `test_browser_auth_site_contract.py`, not mislabeled as the native `__Host-session` name-mismatch case. These results remain pending independent descendant review and are not production evidence.

## Secret-output evidence correction (site descendant)

The prior `cb76e94` captured-output claim above was unsupported: the native canary accepted `capsys` but did not read it. The canary now checks the random local-only cookie value against each manager subprocess's captured stdout and stderr, pytest-captured stdout/stderr on **every** return path (including rejects and interrupted transfers), and bounded fixture/diagnostic text files: `state/*.json`, `state/startup/*/*.json`, and `.json`/`.jsonl`/`.log`/`.txt` under disposable `artifacts` and `shared` (maximum 256 files and 1 MiB each; exceeds bound rather than silently skipping). It does not claim all output channels or private browser-state absence: Chrome's isolated profile must contain the cookie; arbitrary systemd journal, SQLite, binary files, and preserved startup evidence outside the disposable root are not scanned. The check is for the generated cookie value, not every possible credential. No production/site account was used.

From this worktree with `PYTHONPATH=$PWD` and `$TMPDIR/bbh-site-contract-venv/bin/python`: the eleven-file focused suite, split into three bounded runs after a 300-second combined timeout, yielded **10 passed + 29 subtests**, **44 passed**, and **103 passed / 4 skipped** (157 passed total). `BBH_AUTH_CANARY=1` native canary: ordinary success **1 passed**; rejection/end/commit/unproved-begin batch **7 passed**; remaining begin/activation/finalize batch **4 passed, 1 failed** due to a disposable Chrome startup timeout at `request` before the transfer assertion; isolated rerun of that `activate-fence` case **1 passed**. All 13 parameter cases passed at least once with the new assertions; no claim of one clean complete suite invocation under host load. No source behavior, beta ref, Hoster, or live account changed. Independent merge-back was the next gate, not granted by this correction.

## Canonical parent merge-back checkpoint

The reviewed `e32383f` descendant was merged with `--no-ff` into the clean canonical parent at `d3cbde8`, retaining both unique commits (`cb76e94`, `e32383f`); no textual conflict or competing choice arose. The parent's activation/finalize status and terminal public-CDP fence remain unchanged; only the site-qualified fixture adapter, validator, tests and documentation are added. The fetched `origin/beta` was inspected but not merged. Integrated eleven-file focused suite, bounded in three runs: **57 passed / 4 skipped / 29 subtests**, **28 passed**, **70 passed / 13 skipped** (the latter includes 13 opt-in canary skips); total **155 passed, 4 non-canary skips, 29 subtests**, plus 13 canary skips. The 13 opt-in disposable two-Chrome canaries passed in three bounded runs (**4 + 6 + 3**), covering successful transfer, principal/redirect/cookie-name rejection, lost end/commit/begin, activation fence and ACK, and finalize ACK with captured-output secret assertions. Earlier two timed-out broad test attempts were retried in bounded suites; isolated secondary-readback passed and its full 28-case activation suite passed. No Hoster, live account, production grant, beta merge or push. Next: independent combined-tree beta gate and real-site/peer-generation prerequisites; production remains blocked.
