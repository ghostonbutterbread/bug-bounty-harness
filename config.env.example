# Bug Bounty Harness Configuration
# 
# This file is checked into git. Edit values for your system,
# OR set environment variables (they take precedence over this file).
#
# Environment variables override config file values.
# Use : "${VAR:=default}" syntax so env vars always win.

# ============================================================================
# PATHS
# ============================================================================

# Base directory for bounty recon data (per-program folders go here)
# Override with: HARNESS_SHARED_BASE
: "${HARNESS_SHARED_BASE:=${HOME}/Shared/bounty_recon}"

# Root of the bug_bounty_harness repo
# Override with: HARNESS_ROOT
# Root of the bug_bounty_harness repo
# Auto-detected by scripts from script location - don't set here!
# Override with: HARNESS_ROOT env var
# HARNESS_ROOT=

# Wordlists directory
# Override with: HARNESS_WORDLISTS
: "${HARNESS_WORDLISTS:=${HOME}/wordlists}"

# ============================================================================
# PROVIDER SKILL PATHS
# ============================================================================

# Claude Code skills directory
# Override with: CLAUDE_SKILLS_DIR
: "${CLAUDE_SKILLS_DIR:=${HOME}/.claude/skills}"

# Codex skills directory
# Override with: CODEX_SKILLS_DIR
: "${CODEX_SKILLS_DIR:=${HOME}/.agents/skills}"

# Ghost/OpenClaw workspace skills directory
# Override with: GHOST_SKILLS_DIR
: "${GHOST_SKILLS_DIR:=${HOME}/.openclaw/workspace/skills}"

# ============================================================================
# PROVIDER CONFIG DIRECTORIES
# ============================================================================

# Claude Code config directory
: "${CLAUDE_CONFIG_DIR:=${HOME}/.claude}"

# Codex config directory
: "${CODEX_CONFIG_DIR:=${HOME}/.codex}"

# ============================================================================
# MCP / PROXY
# ============================================================================

# Caido MCP proxy URL for proxy-driven workflow mapping
# Override with: KAIDO_MCP_PROXY_URL
: "${KAIDO_MCP_PROXY_URL:=http://127.0.0.1:3333/mcp}"

# ============================================================================
# GIT
# ============================================================================

# GitHub repo URL (for cloning/pushing)
GITHUB_REPO="https://github.com/ghostonbutterbread/bug-bounty-harness.git"

# ============================================================================
# RATE LIMITING
# ============================================================================

# Default requests per second
DEFAULT_RATE_LIMIT=10

# Default concurrent threads
DEFAULT_THREADS=20

# ============================================================================
# HYBRID AGENT ORCHESTRATION
# ============================================================================

# Default planner/worker model choices for agents/hybrid_runner.py.
# CLI flags override these through the runner's resolved config path.
: "${BBH_HYBRID_PLANNER_ENGINE:=codex}"
: "${BBH_HYBRID_PLANNER_MODEL:=gpt-5.5}"
: "${BBH_HYBRID_WORKER_ENGINE:=opencode}"
: "${BBH_HYBRID_WORKER_MODEL:=opencode/deepseek-v4-flash-free}"

# Optional JSON/YAML config path for hybrid runner defaults.
# Example fields: planner, worker, reviewer, max_requests_per_worker,
# worker_limit, rate_limit_rps, browser_escalation, lanes.
# BBH_HYBRID_CONFIG="${HOME}/.config/bug-bounty-harness/hybrid.json"
