# Cross-program browser-profile history false positives

- Branch: `fix/cross-program-profile-history`; worktree: `/home/ryushe/worktrees/bbh-cross-program-profile-history`; base: `4998839964166054e6193939e4e9335be2137d40` (`origin/beta`); target: `beta`.
- Intent: prevent unrelated malformed manager rows from globally vetoing legacy-profile reuse while retaining cross-program physical-profile alias protection. Inspired by read-only Hoster audit: 245 unidentifiable non-terminal rows, many from historical positional inserts. No Hoster DB or live browser mutation in this change.
- Contract: scan all programs for resolvable physical aliases; recover the historical shifted profile path from `launch_file` only for the recognizable positional shape; retain fail-closed unobservable history within the requesting program. Unknown unrelated-program rows no longer veto selection. Canonical lease transaction remains the authoritative acquisition gate.
- Evidence: regression `test_unrelated_program_with_unobservable_history_does_not_block_migration` was red before production edit; the shifted alias test uses an ALTER TABLE-upgraded physical layout and asserts cross-program physical match. Five-file focused suite: 193 passed in 165.36s after the final test fixture change; the two new regressions reran independently (2 passed); `git diff --check` clean. Independent review approved with caveats: the unknown unrelated-row fallback no longer independently proves absence of alias; canonical leases and profile-lock/process checks remain important. The suggested upgraded-layout fixture gap was closed before commit.
- Safety/activation: no historical rows are altered; this does not repair Hoster Neon Blue's own malformed cohort or authorize migration there. Do not activate a live Hoster runtime or overwrite a live profile as part of source integration. The active Blue clone-pool feature is separate and may modify the same provisioner; reconcile before beta merge.
- Next: rerun regression, independent diff review, resolve findings, commit and integrate to beta only if clean/reconciled; verify integrated focused tests. Hoster activation is a separate gated operation.
