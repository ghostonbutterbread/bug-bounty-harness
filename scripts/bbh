#!/usr/bin/env bash
# Resolve this launcher physically so a symlink selects its checkout.
set -euo pipefail
SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd "$(dirname "$SCRIPT_PATH")" && pwd)"
exec python3 "$SCRIPT_DIR/bbh.py" "$@"
