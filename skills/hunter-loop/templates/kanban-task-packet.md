# Hunter Loop Kanban Task Packet

> Copy this into the card body. Delete placeholder guidance only after every
> required field has a concrete answer.

## Objective

<!-- One bounded outcome, not "find vulnerabilities." -->

## Program and owned surface

- Program:
- Task/run ID:
- Section/flow:
- Full URLs/routes:
- Lease owner and expiry/checkpoint:

## Fresh evidence or structural plausibility that justified this card

- Current-run observation:
- Evidence/artifact pointer:
- Why this is not merely a historical MapStore lead:

## Hypothesis ledger

- Active hypothesis and behavioral chain:
- Minimum sufficient discriminating test:
- Expected outcomes and next interpretation:
- Other viable hypotheses / deferred angles and wake conditions:
- Why the test is in scope and what would require a new card or approval:

## Scope and policy chain

- Scope/rules source:
- Required policies/skills:
- Rate/request constraints:
- Account/resource boundary and approved alias/seed reference:
- Owned/disposable impact edge to prove if the active primitive is reproduced:
- Normal owned action permitted for that proof (create/edit/delete/revoke/reset/read),
  its expected interpretations, and cleanup/residue plan:
- Cleanup requirement:

## In scope for this worker

-

## Explicitly out of scope

- Do not expand into a different flow, vulnerability class, or host without a
  new card.
- Do not access or mutate non-owned/unclear user resources.
- Do not paste raw secrets, cookies, bearer tokens, CSRF values, reset links,
  or private headers into the card, comments, artifacts, or result.
-

## Starting context

- Concrete request/response, browser, or map facts:
- Targeted MapStore lookup and intent (`app-facts`, `dedupe`, or `coverage`):
- Prior attempts/constraints relevant to this exact surface:
- Surface lens: `new-surface` | `identity-authority` | `lifecycle` |
  `client-consumer` | `parser-cache` | `integration-downstream` |
  `technology-config` | `offline-analysis`
- Steward continuity: `new steward` | `continue <prior-run-or-card-id>` |
  `independent verifier`
- Why this is fresh work rather than a duplicate retest:

## Execution contract

- Attempts directory:
- Workspace/artifact reference:
- Maximum runtime:
- Stop condition:
- Evidence standard for `confirmed`:
- Approval/human-handoff condition:

## Required result

- Verdict: `confirmed` | `interesting_signal` | `tested_no_signal` | `blocked` | `needs_followup`
- Fresh observations made:
- What was tested and what changed:
- Active hypothesis outcome; deferred/combined/disproved hypothesis updates:
- Pressure state: `cold` | `warm` | `hot` | `exhausted`
- Evidence/artifact pointers:
- Learned constraints / representative failed families:
- MapStore proposal or stable fact written:
- Bounty Notes/handoff needed:
- One recommended next action or explicit reason to stop:
