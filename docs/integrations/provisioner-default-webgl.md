# Provisioner default headed WebGL

- Branch `fix/provisioner-default-webgl`, worktree `/home/ryushe/worktrees/bbh-default-webgl`, base `313b2e8751e9582fbeaa5ebb0b8e5d8cdca2dfb4` (origin/beta), target `beta`.
- Goal: ordinary headed Hoster provisioner requests expose in-page WebGL without setting a wrapper in shared user-manager environment. No target application requests, credential use, or existing browser restart.
- Contract: headed `chromium_test.py` supplies ANGLE/GL flags for normal browser selection, including environment-selected binaries and shell launchers. Explicit `--chrome-binary` or task-scoped `--graphics-backend external` retains custom GL/Vulkan flags; headless is unchanged. The graphics option travels request → start → launcher. `--display-backend auto` remains display selection, not a GPU proof. Agent guidance requires wrapper users to opt out explicitly and verifies canvas WebGL versus hardware GPU.
- Red/green: default headed GL and reviewer-found precedence tests failed before correction. Later review exposed the unsound filename/shebang inference: a plain shell launcher lost GL flags. Its regression failed before replacing inference with a consistent default plus explicit external opt-out. Latest launcher suite: 45 passed in 0.86s; targeted provisioner forwarding: 2 passed in 2.07s; `git diff --check` clean. Broad combined suite timed out after partial progress; do not claim it passed. Final review pending.
- Hoster rollout gate: inspect selected active checkout, service ownership, dependency and skill projection; fast-forward only a clean runtime after beta publication. Run one disposable headed KasmVNC provisioner request with no wrapper environment, inspect private launch command and in-page WebGL, then exact lease/CDP/profile cleanup. Existing Blue browser and historical rows are out of scope; do not restart/migrate them.
- Next: independent review, resolve findings, commit feature; integrate/publish beta; safely activate Hoster, verify projection and real producer fixture. If rollout gate fails, retain feature/beta evidence and report exact blocker rather than claiming deployment.
